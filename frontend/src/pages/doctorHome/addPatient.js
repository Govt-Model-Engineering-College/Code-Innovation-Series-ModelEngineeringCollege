import { Card, CardContent } from "@material-ui/core";
import React from "react";

export default function AddPatient() {
  return (
    <Card>
      <CardContent>Add Patient Details</CardContent>
    </Card>
  );
}
